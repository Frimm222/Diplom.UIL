<Window x:Class="Diplom.UIL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Diplom.UIL"
        xmlns:vm="clr-namespace:Diplom.UIL.ViewModels"
        Title="Склад" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <DockPanel>
        <ListView Name="listView" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, UpdateSourceTrigger=PropertyChanged}" SelectionMode="Single" DockPanel.Dock="Top" Height="300">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Товар"
                        DisplayMemberBinding="{Binding name}" />
                    <GridViewColumn Header="Категория"
                        DisplayMemberBinding="{Binding category_id}" />
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Height="50">
            <Button Content="Добавить" Command="{Binding AddItemCommand}" />
            <Button Content="Удалить" Command="{Binding DeleteItemCommand}" />
            <Button Content="Изменить" Command="{Binding EditItemCommand}" />
        </StackPanel>
    </DockPanel>
    
</Window>
